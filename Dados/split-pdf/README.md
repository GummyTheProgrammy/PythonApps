# split-pdf

Utilities for splitting PDF files into smaller segments.

This folder is intended for batch document splitting workflows and may export individual pages or grouped PDFs. Files usually handled here include `.pdf` and generated `.zip` archives.
