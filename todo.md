# Todo: empacotamento em EEL e distribuição .exe

## Checklist geral

- [x] `weekly-progress` — já empacotado em `.exe`
- [x] `XMLNexus` — já empacotado em `.exe`
- [ ] `Counter` — empacotar para distribuição `.exe`
- [ ] `GhostMouse` — empacotar para distribuição `.exe`
- [ ] `VideoTools` — empacotar para distribuição `.exe`
- [ ] `Video Cutter` — empacotar para distribuição `.exe`
- [ ] `ImageTools` — empacotar para distribuição `.exe`
- [ ] `AudioTools` — empacotar para distribuição `.exe`
- [ ] `File Hunter Nexus` — empacotar para distribuição `.exe`
- [ ] `FolderSizeNexus` — empacotar para distribuição `.exe`
- [ ] `QR CODE GEN` — empacotar para distribuição `.exe`
- [ ] `limpa_csv` — empacotar para distribuição `.exe`
- [ ] `split pdf` — empacotar para distribuição `.exe`
- [ ] `Gadgets` / utilitários gerais — revisar se ainda precisam de empacotamento

## Observações

- `EcomReport` foi removido da lista porque está deprecated e fora do escopo atual.
- Manter o foco em empacotar um programa por vez e validar cada `.exe` gerado.
